import { Button } from "@mui/material";

export default function FacebookButton() {
  return (
    <Button
      fullWidth
      color="info"
      size="large"
      variant="contained"
      sx={{ borderRadius: 3 }}
    >
      Facebook
    </Button>
  );
}
